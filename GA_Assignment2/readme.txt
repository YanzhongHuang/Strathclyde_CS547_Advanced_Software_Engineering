1.将数据和脚本放在同一目录下
2.确保安装了python3.7和模块numpy,进入脚本对应目录运行"python GA2.py"
3.数组ga_small，hc_small等表示对应方法所求出的最优排列，这个脚本只是用两种方法对两个数据集分别运行一次，在评估时会运行多次。

1.Make sure the data file and the script are in the same path.
2.Run this script by typing "python GA2.py" in windows CMD or linux shell, make sure you have installed python 3.7 and module numpy.
3.The permutations in ga_small and other lists is the are the best solution for each algorithm, this script is only 1 run for each algorithm and each dataset, more test will be done for the evaluation.